export type ItemQuestionType = {
  it_title: string;
  // quest_no: string;
  // total_no: string;
  // it_quest_context_p: string | null;
  // it_code: string;
  // doc_num: string;
  it_answer: string[];
  // type: string;
  // it_subject: string;
  // it_tip: string;
  // it_class: string;
  // it_id: string;
  // group_id: string | null;
  // it_season: string;
  it_quest: string;
  // it_point: string;
  // it_hint: string | null;
};
