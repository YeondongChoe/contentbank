import * as React from 'react';

import styled from 'styled-components';

import { CreatingSetting } from '../../components';

export function CreatingContentSetting() {
  return (
    <Container>
      <CreatingSetting />
    </Container>
  );
}

const Container = styled.div``;
