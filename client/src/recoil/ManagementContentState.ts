import { atom } from 'recoil';

export const ManagementContentPopupState = atom({
  key: 'ManagementContentPopupState',
  default: false,
});

export const ManagementTreePopupState = atom({
  key: 'ManagementTreePopupState',
  default: false,
});
